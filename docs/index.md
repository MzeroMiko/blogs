# Reading Notes

Hi, I am [MzeroMiko](https://mzeromiko.github.io). This site is built with MkDocs. To replicate this setup, simply run `pip install mkdocs-material` and use `mkdocs serve` for local preview.

